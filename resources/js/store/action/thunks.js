import HTTP from '../../utils/HTTP';

export const fetchActions = (salon_id) => (dispatch, getState) => {
    HTTP.get('/')
}

export const fetchActionsOfSalon = (salon_id) => (dispatch, getState) => {
    HTTP.get('/')
}