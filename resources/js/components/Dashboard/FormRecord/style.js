import { makeStyles } from '@material-ui/core/styles';

const styleSelect = makeStyles({

    root: {
        width: '100%',
    },
    label: {
        width: '100%'
    }
});

export default styleSelect;