import { makeStyles } from '@material-ui/core/styles';

export const formLogin = makeStyles((theme) => ({
  
    root: {
      display: 'flex',
      flexDirection: 'column',
      justifyContent: 'center'
    },
    item: {
      width: '100%'
    },
  }));