import { makeStyles } from '@material-ui/core/styles';

const styleServices = makeStyles({
    root: {
        'justify-content': 'space-between',
    },
    addBtn: {
        width: '100%'
    }
});

export default styleServices;