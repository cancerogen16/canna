import { makeStyles } from '@material-ui/core/styles';

const styleMasters = makeStyles({
    root: {
        display: 'flex',
        'justify-content': 'space-between',
        flexDirection: 'column',
    },
    addBtn: {
      width: '100%'
    }
  });

export default styleMasters;